# Runtime dependencies (installed into Blender's Python)
# Blender 4.0+ bundles Python 3.10+ with numpy pre-installed
Pillow>=10.0
opencv-python>=4.8
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
psd-tools>=2.0

# 2D pose estimation enrichment (run_enrich.py)
rtmlib>=0.1.0
onnxruntime>=1.16

# Background removal
rembg>=2.0

# Gemini API (image generation for gemini_diverse dataset)
google-genai>=1.0

# Manual annotation pipeline (Label Studio server — install separately)
# pip install label-studio
